{
	"watch": ["src/**/*.ts"],
	"ext": "ts",
	"ignore": ["src/**/*.spec.ts"],
	"exec": "node --max-old-space-size=512 -r ts-node/register -r dotenv/config -r tsconfig-paths/register src/server.ts",
	"legacyWatch": true,
	"env": {
		"NODE_ENV": "dev"
	}
}
